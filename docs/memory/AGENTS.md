# 项目研究记忆约束

- 本目录由研究设计 agent 维护；实验执行 agent 只读。
- `CURRENT_STATE.md` 只保存当前有效口径和可定位状态，不保存完整聊天转录。
- 实验事实只有在任务卡、结果报告、原始证据和研究审查齐全后才能写入。
- 未经审查的运行、聊天摘要和全局 Codex memory 只能作为待核验线索，不能覆盖 Git 中的当前文件。
- 重大方向变更写入 `docs/decisions/`，不得通过静默改写当前状态抹除历史。
- 每次记忆更新必须形成独立研究分支 commit，并注明来源、证据状态和被替换口径。
