# 实验设计审查：PRT-002 (PDD 模块准入审查)

## 1. 审查结论

- 设计状态：`APPROVED`
- 审查人：研究设计 agent
- 审查日期：2026-08-21
- 任务卡及版本：`experiment_handoffs/tasks/PRT-002-pdd-module-and-ablation.md` (v1.0)
- 当前允许阶段：`Phase 1: PDD 模块实现、单元测试与 Gate S 模型 Smoke 连线验证`
- 下一任务：`LOCKED` (PRT-003 SSR 保持锁定)

## 2. 研究识别性检查

| 审查项 | 结论 | 依据/修改要求 |
|---|---|---|
| 唯一研究问题清晰 | **通过** | 明确解决向浅层 P2/P3 供给特征的早期下采样中 2–8 px 局部细节丢失问题 |
| 假设可被否证 | **通过** | 若 PDD 相对等容量/等参数基线无法稳定提升 APvt (+0.6)，则判定假设不成立并废弃模块 |
| 单次核心变量受控 | **通过** | 仅修改早期下采样机制（PDD），保持 FPN、Head、损失函数与优化器完全一致 |
| 对照容量/训练公平 | **通过** | 设立 Space-to-Depth 仅有、DWConv 仅有及参数量匹配基线对照 |
| 指标与主张对应 | **通过** | 主指标锁定 APvt / ARvt，结合 2–4 px, 4–6 px 细分诊断箱 |
| 无数据/标签/test泄漏 | **通过** | 严格仅在 train 训练、val 验证，test 保持物理隔离 |
| 结果非定义机械保证 | **通过** | PDD 融合可能引入额外高频冗余，能否提升 AP 取决于实测特征有效性 |
| 失败条件可指导下一步 | **通过** | 设立明确停止条件：无稳定增益时删除 PDD，不强行保留 |
| 资源成本与信息价值匹配 | **通过** | 参数量增幅受控在 3% 以内，先执行 Phase 1 的 CPU/GPU Smoke 验证 |

## 3. 固定项与允许范围

- **固定项**：AI-TOD-v2 官方 train/val split；FCOS-P2 架构；12 epochs 训练超参；
- **允许实验 Agent 自主范围**：
  1. 在 `src/prtiny/models/pdd.py` 中编写 `PDDDownsample` 与集成逻辑；
  2. 编写 `tests/test_pdd.py` 与 `tools/smoke_pdd.py`；
  3. 自主排查并修复张量维度拼接、通道对齐与数值稳定性问题；
  4. 生成 Gate S Smoke 连线产物与自我审查报告。
- **禁止修改**：禁止引入 SSR 频谱增强；禁止引入 NWD；禁止修改主指标定义。

## 4. 执行授权

- **是否允许实验 agent 启动**：`YES`
- **放行签名**：
  `[DESIGN_APPROVED][PRT-002] 阶段方案准入审查通过，授权实验执行 Agent 启动 Phase 1 工程实现、PDD 模块编写、单元测试与 Gate S Smoke 连线验证。`
